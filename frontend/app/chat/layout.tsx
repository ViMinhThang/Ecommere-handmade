import { tabMetadata } from "@/lib/page-titles";

export const metadata = tabMetadata("Tin nhắn | The Artisanal Curator");

export default function ChatLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

import { tabMetadata } from "@/lib/page-titles";

export const metadata = tabMetadata("Độ tin cậy thanh toán | Bảng điều khiển");

export default function DashboardPaymentsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

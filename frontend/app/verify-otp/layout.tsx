import { tabMetadata } from "@/lib/page-titles";

export const metadata = tabMetadata("Xác minh OTP | The Artisanal Curator");

export default function VerifyOtpLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

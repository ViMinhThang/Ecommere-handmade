import { tabMetadata } from "@/lib/page-titles";

export const metadata = tabMetadata("Commission | Bảng điều khiển");

export default function SellerCommissionsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

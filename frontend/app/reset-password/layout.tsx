import { tabMetadata } from "@/lib/page-titles";

export const metadata = tabMetadata("Đặt lại mật khẩu | The Artisanal Curator");

export default function ResetPasswordLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

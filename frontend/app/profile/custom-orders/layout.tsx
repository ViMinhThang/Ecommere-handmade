import { tabMetadata } from "@/lib/page-titles";

export const metadata = tabMetadata("Đơn thiết kế riêng | Tài khoản");

export default function ProfileCustomOrdersLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

import { tabMetadata } from "@/lib/page-titles";

export const metadata = tabMetadata(
  "Gian hàng handmade | The Artisanal Curator",
);

export default function SellersLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

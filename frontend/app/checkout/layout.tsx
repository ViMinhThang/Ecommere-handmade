import { tabMetadata } from "@/lib/page-titles";

export const metadata = tabMetadata("Thanh toán | The Artisanal Curator");

export default function CheckoutLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
